# News Sentiment and Market Dynamics — BTC / GLD

Predicting next-day volatility spikes in Bitcoin and Gold (GLD ETF) from
news sentiment, July–December 2024.

## Datasets

| File | Rows | Description |
|---|---|---|
| `normalized_news_july_dec_2024.csv` | ~21K | News headlines tagged `financial`, `political`, or both; 50+ sources |
| `bitcoin_prices_2024_4h.csv` | 1,098 | BTC OHLCV at 4-hour resolution |
| `GLD_ETF_Stock_Price_History_Converted.csv` | 128 | GLD ETF daily OHLCV |

## Pipeline

1. **Pseudo-label** a stratified 5K sample of headlines with zero-shot
   BART-MNLI using market-framed hypotheses.
2. **Filter** by confidence (≥ 0.65) — keeps ~2.9K high-quality labels.
3. **Fine-tune** FinancialBERT (bottom 4 encoder layers + embeddings
   frozen) for 4 epochs on the pseudo-labeled set.
4. **Score** all 21K headlines with the fine-tuned model.
5. **Aggregate** to daily sentiment features, split into three parallel
   streams: `all_*`, `fin_*` (financial-tagged), `pol_*` (political-tagged).
6. **Merge** with BTC / GLD daily OHLCV and train spike-prediction models
   (next step — see `finetune_financialbert.py` for stages 1–5).

Run:

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install --upgrade torch torchvision torchaudio
pip install transformers datasets scikit-learn pandas numpy accelerate
python finetune_financialbert.py
```

On an Apple Silicon M5 Pro / 48GB the full pipeline finishes in ~10 min
using MPS acceleration.

## Results — Fine-tuning Ablation

Held-out test set of 291 headlines (binary positive / negative, since
zero-shot pseudo-labeling produced < 1% confident "neutral" examples).

| Model | Accuracy | F1 (macro) |
|---|---|---|
| FinancialBERT (off-the-shelf) | 0.526 | 0.423 |
| **FinancialBERT (fine-tuned)** | **0.790** | **0.790** |

### Per-class breakdown (fine-tuned)

```
              precision    recall  f1-score   support
    negative      0.809     0.770     0.789       148
    positive      0.773     0.811     0.792       143
    accuracy                          0.790       291
   macro avg      0.791     0.791     0.790       291
```

### Confusion matrix (fine-tuned)

|          | pred neg | pred pos |
|----------|---------:|---------:|
| **true neg** | 114 | 34 |
| **true pos** | 27 | 116 |

The baseline collapses onto "negative" (138/148 true negatives caught,
only 15/143 true positives) because FinancialBERT's pre-training label
distribution is very different from the market-framed zero-shot
hypotheses used for pseudo-labeling. Fine-tuning aligns the head to the
target labeling scheme — this is a scheme-alignment gain, not a proof
of superhuman sentiment accuracy.

## Artifacts

Generated by running `finetune_financialbert.py` (not committed; see
Releases for the fine-tuned model):

```
artifacts/
├── pseudo_labeled_sample.csv   # zero-shot labels + confidences
├── ablation_results.csv        # baseline vs fine-tuned metrics
├── news_scored.csv             # all 21K headlines with sentiment scores
├── daily_sentiment.csv         # 183 days × 15 features, ready for modeling
└── finbert-ft-best/            # fine-tuned model (released separately)
```

## Releases

The fine-tuned model (~419 MB) is published as a GitHub Release — too
large to commit directly. Download and extract into `artifacts/` to reuse
without re-running the fine-tune step.

## Results — 2×3 Matrix (Walk-Forward CV)

Produced by `analyze_and_model.py`. Best model per cell (LogReg / RF / XGBoost).
All cells evaluated on the same 182 aligned daily rows (sentiment + BTC price);
GLD cells use only trading days.

### σ = 2.0 (strict: spike rate ~14%)

| AUC | Financial only | Political only | Combined |
|-----|---------------:|---------------:|---------:|
| BTC | 0.608          | 0.538          | **0.631** |
| GLD | 0.464          | 0.546          | 0.563 |

### σ = 1.5 (lenient: spike rate ~20%)

| AUC | Financial only | Political only | Combined |
|-----|---------------:|---------------:|---------:|
| BTC | **0.623**      | 0.490          | 0.569 |
| GLD | 0.537          | 0.447          | 0.512 |

### Interpretation

- **Bitcoin has a modest but consistent sentiment signal.** Financial news
  alone reaches AUC 0.61–0.62; combining all sentiment streams at σ=2.0
  pushes to 0.63. Effect is small but robust across thresholds.
- **Gold signal is near-chance.** No configuration clears AUC 0.57 for
  GLD. Headline sentiment (especially from a corpus dominated by
  cryptocurrency outlets) does not meaningfully predict gold volatility
  over this six-month window.
- **Political news on its own does not predict either asset.** The pol
  stream is sparse (~8% of headlines) and may be too noisy day-to-day.
  Its signal only appears when combined with financial sentiment for BTC.

F1 scores are low (max 0.40) because spikes are rare by construction;
AUC is the primary metric for this class imbalance.

## Artifacts Generated

```
artifacts/
├── merged_daily_sig2p0.csv, merged_daily_sig1p5.csv
├── results_matrix_{f1,auc}_sig{2p0,1p5}.csv     ← 2×3 matrices
├── results_full_sig{2p0,1p5}.csv                ← every model × cell
├── granger_results_sig{2p0,1p5}.csv
└── plots/
    ├── price_vs_sentiment.png
    ├── correlation_heatmap_sig{2p0,1p5}.png
    ├── model_comparison_{btc,gld}_sig{2p0,1p5}.png
    └── feature_importance_{btc,gld}_sig{2p0,1p5}.png
```
